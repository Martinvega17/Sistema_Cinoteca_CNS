# Cuando conecten MongoDB Atlas más adelante, peguen aquí su cadena de
# conexión (Atlas → Connect → Drivers) y agreguen esta misma variable en
# Vercel: Project → Settings → Environment Variables.
MONGODB_URI=
